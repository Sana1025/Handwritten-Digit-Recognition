# Handwritten-Digit-Recognition
Neural network built from scratch using NumPy for handwritten digit recognition on MNIST. Implements forward/backpropagation, mini-batch gradient descent, ReLU and Softmax activations, achieving ~97% test accuracy with visualization of training metrics and predictions.
